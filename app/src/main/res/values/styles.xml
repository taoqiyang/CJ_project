<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">#ff00ff00</item>
        <item name="base_background">#fffefefe</item>
    </style>

    <style name="AppTheme_Night" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@android:color/background_dark</item>
        <item name="base_background">@android:color/background_dark</item>
    </style>

</resources>
